# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
SESSION_COOKIE_AGE=3600

# Database (leave empty to use SQLite)
# DATABASE_URL=postgres://user:password@localhost:5432/visitor_db

# CORS (frontend dev server)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: Fernet key for national ID encryption (base64). Leave empty to derive from SECRET_KEY.
# NATIONAL_ID_ENCRYPTION_KEY=
